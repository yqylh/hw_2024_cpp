#include "config.hpp"
#include "input.hpp"

int main() {
    inputMap();
    while (1) {
        // inputFrame();
        // if (inputFlag == false) break;
        solveFrame();
    }
    return 0;
}
